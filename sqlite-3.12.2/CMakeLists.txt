CMAKE_MINIMUM_REQUIRED(VERSION 2.8.11)

set(MODULE_SRCPATH $ENV{MODULE_SRCPATH})

#set(EXECUTABLE_OUTPUT_PATH  "${OUTDIR}/bin")

AUX_SOURCE_DIRECTORY(${MODULE_SRCPATH} DIR_SRCS)
add_library(sqlite ${DIR_SRCS})

